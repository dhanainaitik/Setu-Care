{
  "name": "setucare",
  "version": "1.0.0",
  "description": "SetuCare — AI-Powered Digital Healthcare Platform",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend:https": "npm run dev:https --prefix frontend",
    "backend:https": "npm run dev:https --prefix backend",
    "dev:https": "echo Start both manually in separate terminals: (1) npm run backend:https  (2) npm run frontend:https"
  }
}
